# ECE-9413 Assignment 2: SumCheck Prover in JAX

## What This Is

An implementation of the prover-side logic for the SumCheck protocol using JAX.
The prover takes a multilinear polynomial expressed as an evaluation table, runs
`num_rounds` rounds of the protocol (each folding one Boolean variable with a random
challenge), and returns the initial claim together with the per-round univariate
evaluations.

## Core Value

Correctly produce `(claim0, round_evals)` that passes the harness for all required
32-bit base expressions (`a`, `a*b`, `a*b+c`, `a*b*c`) on vars4, vars16, and vars20.

## Requirements

### Validated

- [x] `mod_add_32 / mod_sub_32 / mod_mul_32` — correct modular arithmetic over 32-bit primes (Validated in Phase 1: Primitives + MLE Update, 30/30 tests pass)
- [x] `mle_update_32` — linear interpolation that folds one Boolean variable in an MLE table (Validated in Phase 1: textbook example F_17 confirmed)

### Validated

- [x] `sumcheck_32` — full prover loop: computes round evaluations and initial claim (Validated in Phase 2: 91 tests pass on vars4/vars16/vars20)
- [x] Pass all required 32-bit tests (vars4, vars16, vars20) for base polynomials (Validated in Phase 2: all 60 case tests pass)
- [x] Produce required benchmark results (vars4, vars16, vars20) (Validated in Phase 3: benchmarks ran cleanly, timing captured)
- [x] Submission package (`code.zip`) generated by `make_submission.sh` (Validated in Phase 3: code.zip contains only student.py)

### Active
- [ ] Extra credit: advanced 32-bit polynomials (`a*a*b*b*c`, `a*b*c+d*e`, `a*b*c*g+d*e*g`)

### Out of Scope

- 64-bit and 128-bit tracks — not required; defer until required track passes
- OAuth / auth — not applicable
- Real-time communication — not applicable

## Context

- **Harness**: `student.py` is the only file students edit; the harness in `provided.py`,
  `sumcheck_utils.py`, and `tests/` is read-only.
- **Expression format**: `list[list[str]]` — outer list is additive terms, inner is
  multiplicative factors. E.g., `a*b + c` → `[["a", "b"], ["c"]]`.
- **eval_tables**: dict from variable name → JAX array of MLE evaluations.
- **Challenges**: `num_rounds - 1` values passed to `sumcheck_32` (final verifier-only
  challenge excluded).
- **Rounds must be serialized**: cannot parallelise across rounds.
- **Prime**: `MAX_PRIME_32 = 4294967291` fits in uint32, but intermediate products
  can overflow — must use uint64 for multiplication then reduce.
- **Debug tool**: `provided.expression_round_trace()` available for vars4 only.
- **JAX x64**: `jax_enable_x64 = True` is set globally; use `jnp.uint64` or
  `jnp.int64` for intermediates.
- **Platform**: default CPU; GPU/TPU selectable via `JAX_PLATFORMS`.

## Constraints

- **API**: Frozen dispatchers (`sumcheck`, `mod_add`, …) must not be renamed or changed.
- **File**: Only `student.py` is edited.
- **Timeline**: Due within 1 week.
- **Round serialization**: Each round depends on the previous challenge — cannot be
  parallelized at the round level (but intra-round vectorization with `vmap`/`jit` is fine).
- **Performance target**: JAX JIT + vmap — design for GPU/TPU from the start.

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Use uint64 intermediate for all 32-bit arithmetic | 32-bit × 32-bit can overflow uint32; sum of two (q-1) values also overflows — wider accumulator required | Applied in Phase 1 (D-01) |
| mod_sub_32 uses (a+q-b) not (a-b+q) | (a-b) as uint64 underflows to ~2^64 when b > a; adding q first keeps the intermediate positive | Applied in Phase 1 (D-02) |
| mle_update_32 composes three primitives | Reusing mod_sub/mul/add avoids duplicating uint64 promotion logic | Applied in Phase 1 (D-03) |
| No jax.vmap in mle_update_32 | JAX/XLA already broadcasts array operations; vmap adds overhead with no benefit | Applied in Phase 1 |
| vmap over table entries within a round | Parallelise the inner-loop work that is independent across table indices | Not needed — JAX/XLA vectorizes array ops automatically |
| Correctness-first, then jit | Easier to debug before adding jit/vmap; avoid retracing issues | Applied in Phase 3 |
| expression must be tuple-of-tuples at JIT boundary | list[list[str]] is unhashable; JAX requires static args to be hashable | Applied in Phase 3 (D-05): converted in sumcheck() dispatcher |
| JIT on sumcheck_32 (inner); harness applies outer JIT | Nested JIT inlined by XLA — no double-compilation; inner specializes on expression structure | Applied in Phase 3 (D-06) |

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition:**
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone:**
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-05-03 — Phase 3 complete — all 3 phases done, code.zip ready for submission*
