---
phase: 03-jax-optimization-submission
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified: []
autonomous: true
requirements:
  - SUB-01
must_haves:
  truths:
    - "make_submission.sh exits with code 0"
    - "code.zip exists in the repository root after the script runs"
    - "code.zip contains only student.py (the only submittable file)"
  artifacts:
    - path: "code.zip"
      provides: "Submission archive for Brightspace upload"
      contains: "student.py"
  key_links:
    - from: "scripts/make_submission.sh"
      to: "code.zip"
      via: "zip -j code.zip student.py"
      pattern: "code\\.zip"
---

<objective>
Run `bash scripts/make_submission.sh` to produce the final `code.zip` submission artifact. The script re-runs all required tests internally before packaging — a clean exit guarantees the submitted file is correct.

Purpose: Satisfies SUB-01. The zip contains only `student.py` and is ready for direct upload to Brightspace.

Output: `code.zip` at the repository root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/oud/Projects/ECE-9413/assignment2/.planning/ROADMAP.md
@/Users/oud/Projects/ECE-9413/assignment2/.planning/STATE.md
</context>

<interfaces>
<!-- What make_submission.sh does (read from scripts/make_submission.sh): -->
<!--
  1. cd to repo root
  2. uv run pytest --all-32 --num-vars 4 --num-vars 16 --num-vars 20   (exits non-zero if any test fails)
  3. rm -f code.zip
  4. zip -j code.zip student.py
  5. prints "Done: code.zip" and "Upload code.zip to Brightspace."
-->
<!-- If step 2 fails, the script exits immediately (set -euo pipefail). Fix student.py first. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run make_submission.sh and verify code.zip is produced</name>
  <files></files>
  <read_first>
    /Users/oud/Projects/ECE-9413/assignment2/scripts/make_submission.sh
    /Users/oud/Projects/ECE-9413/assignment2/student.py
  </read_first>
  <action>
Run the submission script from the repository root:

```bash
cd /Users/oud/Projects/ECE-9413/assignment2 && bash scripts/make_submission.sh
```

The script will:
1. Run `uv run pytest --all-32 --num-vars 4 --num-vars 16 --num-vars 20` (all required 32-bit tests).
2. If tests pass, zip `student.py` into `code.zip` at the repository root.
3. Print "Done: code.zip" and "Upload code.zip to Brightspace."

If the script exits non-zero (test failure), do NOT attempt to bypass the tests. Instead:
- Inspect the failing test output
- Identify what changed in student.py (the JIT decorator from plan 03-01 is the only recent change)
- If the issue is the JIT decorator, review whether `static_argnames` is correct and whether `q` was accidentally made static
- Fix student.py and re-run the script

After the script succeeds, confirm code.zip exists:
```bash
ls -lh /Users/oud/Projects/ECE-9413/assignment2/code.zip
```

Confirm code.zip contains student.py and nothing else:
```bash
unzip -l /Users/oud/Projects/ECE-9413/assignment2/code.zip
```
  </action>
  <verify>
    <automated>ls -lh /Users/oud/Projects/ECE-9413/assignment2/code.zip && unzip -l /Users/oud/Projects/ECE-9413/assignment2/code.zip</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/make_submission.sh` exits with code 0 (no non-zero exit, no "FAILED" in pytest summary)
    - `ls /Users/oud/Projects/ECE-9413/assignment2/code.zip` finds the file (exit code 0)
    - `unzip -l /Users/oud/Projects/ECE-9413/assignment2/code.zip` output contains exactly one entry: `student.py` (no other files)
    - Script output contains the line "Done: code.zip"
    - Script output contains the line "Upload code.zip to Brightspace."
  </acceptance_criteria>
  <done>code.zip exists at the repository root, contains only student.py, and was produced by make_submission.sh exiting 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| make_submission.sh → pytest | Script runs tests and aborts on failure — protects against submitting a broken implementation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | code.zip contents | mitigate | Verify with `unzip -l code.zip` that only `student.py` is present — no test files, data, or secrets accidentally included |
| T-03-04 | Repudiation | submission artifact | accept | Brightspace submission records upload timestamp; local code.zip is the single source of truth |
</threat_model>

<verification>
```bash
cd /Users/oud/Projects/ECE-9413/assignment2 && bash scripts/make_submission.sh && unzip -l code.zip
```
Script exits 0. `unzip -l` shows exactly one file: `student.py`.
</verification>

<success_criteria>
- `bash scripts/make_submission.sh` exits 0
- `code.zip` exists at `/Users/oud/Projects/ECE-9413/assignment2/code.zip`
- `unzip -l code.zip` shows exactly one entry: `student.py`
- No test failures during the submission run
</success_criteria>

<output>
After completion, create `/Users/oud/Projects/ECE-9413/assignment2/.planning/phases/03-jax-optimization-submission/03-02-SUMMARY.md`
</output>
