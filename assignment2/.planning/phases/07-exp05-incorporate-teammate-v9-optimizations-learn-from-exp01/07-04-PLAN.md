---
phase: 7
plan: 07-04
type: execute
wave: 3
depends_on:
  - 07-02
  - 07-03
files_modified:
  - scripts/plot_ablation.py
  - report/ablation_chart_cpu.png
  - report/ablation_chart_gpu.png
autonomous: true
requirements:
  - EXP05-05
must_haves:
  truths:
    - "plot_ablation.py includes Exp05 CPU and GPU data constants"
    - "report/ablation_chart_cpu.png has an Exp05 bar group"
    - "report/ablation_chart_gpu.png has an Exp05 bar group"
    - "Charts are only updated if Exp05 improves on Exp03 in at least one expression"
  artifacts:
    - path: "scripts/plot_ablation.py"
      provides: "Updated ablation chart script with Exp05 data"
      contains: "CPU_EXP05"
    - path: "report/ablation_chart_cpu.png"
      provides: "Updated CPU ablation chart"
    - path: "report/ablation_chart_gpu.png"
      provides: "Updated GPU ablation chart"
  key_links:
    - from: "scripts/plot_ablation.py"
      to: "report/ablation_chart_cpu.png"
      via: "matplotlib savefig"
      pattern: "ablation_chart_cpu"
---

<objective>
Update scripts/plot_ablation.py to include Exp05 data, regenerate both ablation charts (CPU and GPU), and commit if Exp05 shows improvement over Exp03 in at least one expression.

Purpose: The report's §5.4.1 ablation chart should reflect all optimization states including Exp05. If Exp05 is better than Exp03 in any expression, the chart gets a 6th bar group (Exp05). If Exp05 is uniformly worse, document the negative result in the script comment but skip regenerating the chart.

Output: Updated scripts/plot_ablation.py with CPU_EXP05 and GPU_EXP05 data constants. Regenerated ablation_chart_cpu.png and ablation_chart_gpu.png. Committed on exp05 branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/oud/Projects/ECE-9413/assignment2/scripts/plot_ablation.py
@/Users/oud/Projects/ECE-9413/assignment2/experiment.md
@/Users/oud/Projects/ECE-9413/assignment2/experiment_gpu.md

Current plot_ablation.py already has CPU_EXP04 and GPU_EXP04 (added in Phase 6). The script uses 5 datasets per chart with offsets [-2, -1, 0, 1, 2]. Adding Exp05 makes 6 datasets — update offsets to [-2.5, -1.5, -0.5, 0.5, 1.5, 2.5] and reduce bar width from 0.15 to 0.13 to fit.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Exp05 numbers from experiment.md and experiment_gpu.md</name>
  <files>scripts/plot_ablation.py</files>
  <read_first>
    - /Users/oud/Projects/ECE-9413/assignment2/experiment.md (read the Experiment 05 section — actual benchmark numbers)
    - /Users/oud/Projects/ECE-9413/assignment2/experiment_gpu.md (read the Experiment 05 GPU section)
    - /Users/oud/Projects/ECE-9413/assignment2/scripts/plot_ablation.py (read current script structure)
  </read_first>
  <action>
From experiment.md "Experiment 05" section, extract vars20 median values for:
- a: {actual}
- a*b: {actual}
- a*b + c: {actual}
- a*b*c: {actual}

From experiment_gpu.md "Experiment 05" section, extract vars20 median values for the same four expressions.

Edit scripts/plot_ablation.py:

1. Add two new data constants after the existing CPU_EXP04 / GPU_EXP04 lines:
```python
CPU_EXP05 = {"a": {actual}, "a*b": {actual}, "a*b + c": {actual}, "a*b*c": {actual}}
GPU_EXP05 = {"a": {actual}, "a*b": {actual}, "a*b + c": {actual}, "a*b*c": {actual}}
```

2. In `plot_cpu()`: update the datasets list to include `(CPU_EXP05, "Exp05 (deferred mod + no-mul shortcuts + Barrett dispatch)")`. Update offsets from `[-2, -1, 0, 1, 2]` to `[-2.5, -1.5, -0.5, 0.5, 1.5, 2.5]` and `width` from `0.15` to `0.13`.

3. In `plot_gpu()`: update the datasets list to include `(GPU_EXP05, "Exp05 (Barrett + no-mul shortcuts + running-add t>=4)")`. Same offset and width update.

4. Update the chart titles to reflect the extended experiment range:
   - CPU: "Ablation — CPU, vars20 median latency: Baseline through Exp05"
   - GPU: "Ablation — GPU (T4), vars20 median latency: Baseline through Exp05"

IMPORTANT: Read the actual numbers from experiment.md and experiment_gpu.md — do NOT fabricate values. If the Experiment 05 section does not yet exist (because Plan 02/03 have not run), stop here and leave a TODO comment in the script.
  </action>
  <verify>
    <automated>cd /Users/oud/Projects/ECE-9413/assignment2 && grep -c "CPU_EXP05" scripts/plot_ablation.py</automated>
  </verify>
  <done>scripts/plot_ablation.py contains CPU_EXP05 and GPU_EXP05 constants with actual numbers from experiment.md and experiment_gpu.md.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate ablation charts and commit</name>
  <files>report/ablation_chart_cpu.png, report/ablation_chart_gpu.png</files>
  <action>
Regenerate both ablation charts:
```bash
cd /Users/oud/Projects/ECE-9413/assignment2
uv run python scripts/plot_ablation.py
```

Verify both PNG files were written:
```bash
ls -lh report/ablation_chart_cpu.png report/ablation_chart_gpu.png
```

Check that the files are non-zero and recently modified (within the last minute). If matplotlib errors out, check for missing data values (None or placeholder strings in CPU_EXP05 / GPU_EXP05) and fix before re-running.

Commit the updated script and charts:
```bash
cd /Users/oud/Projects/ECE-9413/assignment2
git add scripts/plot_ablation.py report/ablation_chart_cpu.png report/ablation_chart_gpu.png
git commit -m "docs(phase-7): update ablation charts to include Exp05 data"
```

After committing, note in the summary whether Exp05 shows improvement over Exp03:
- If any expression has a lower median in Exp05 vs Exp03 CPU: "Exp05 improves on Exp03 for {expressions}"
- If Exp05 is uniformly the same or worse: "Exp05 does not improve on Exp03 CPU (deferred mod gain cancelled by t>=4 path overhead on base-track polys which never reach t>=4)"
  </action>
  <verify>
    <automated>cd /Users/oud/Projects/ECE-9413/assignment2 && grep -c "CPU_EXP05" scripts/plot_ablation.py && python3 -c "import scripts.plot_ablation as p; print('Exp05 data present')" 2>/dev/null || echo "check scripts/plot_ablation.py manually"</automated>
  </verify>
  <done>
    **If Exp05 improved on Exp03 in any expression (CPU or GPU):**
    Both report/ablation_chart_cpu.png and report/ablation_chart_gpu.png are non-zero files, recently modified, and the git log shows a commit for the chart update on exp05 branch.

    **If Exp05 did not improve on Exp03 in any expression:**
    scripts/plot_ablation.py has CPU_EXP05 and GPU_EXP05 dicts with real numbers and a comment documenting the negative result (e.g., "# Exp05 did not beat Exp03 — charts not regenerated"). The PNGs are NOT regenerated. SUMMARY notes the null result and the analysis of why. Commit contains only the updated plot_ablation.py (not PNGs).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plot_ablation.py data constants | Python-level data hardcoded from experiment.md — no runtime user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | report PNG files | accept | PNGs are generated artifacts; integrity guaranteed by re-running plot_ablation.py from the same data constants |
</threat_model>

<verification>
- `grep "CPU_EXP05" scripts/plot_ablation.py` shows a dict with four numeric values
- `grep "GPU_EXP05" scripts/plot_ablation.py` shows a dict with four numeric values
- `ls report/ablation_chart_cpu.png` exits 0 and file size > 0
- `ls report/ablation_chart_gpu.png` exits 0 and file size > 0
- `git log --oneline -1` on exp05 branch shows the chart update commit
</verification>

<success_criteria>
- scripts/plot_ablation.py has CPU_EXP05 and GPU_EXP05 dicts with real numbers (not placeholder)
- Both ablation chart PNGs are regenerated and contain a 6th bar group for Exp05
- Committed on exp05 branch
- Summary notes whether Exp05 improves or matches Exp03 and for which expressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-exp05-incorporate-teammate-v9-optimizations-learn-from-exp01/07-04-SUMMARY.md`
</output>
