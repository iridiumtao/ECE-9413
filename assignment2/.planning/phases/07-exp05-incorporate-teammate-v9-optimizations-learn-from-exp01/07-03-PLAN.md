---
phase: 7
plan: 07-03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - experiment_gpu.md
autonomous: true
requirements:
  - EXP05-04
must_haves:
  truths:
    - "experiment_gpu.md has an Experiment 05 GPU section with T4 results for vars20"
    - "GPU results include Barrett variant numbers (the _sumcheck_32_barrett_exp05 path)"
    - "CPU vs GPU delta table is recorded for Exp05"
    - "Git hash for exp05 commit is recorded in experiment_gpu.md"
  artifacts:
    - path: "experiment_gpu.md"
      provides: "Experiment 05 GPU benchmark results on Modal T4"
      contains: "Experiment 05"
  key_links:
    - from: "experiment_gpu.md"
      to: "exp05 branch HEAD"
      via: "git hash"
      pattern: "Git hash.*[0-9a-f]{8}"
---

<objective>
Run Exp05 on Modal T4 GPU and record results in experiment_gpu.md as the "Experiment 05 GPU" section.

Purpose: On GPU, the Barrett variant (_sumcheck_32_barrett_exp05) is dispatched via adaptive dispatch. The key question is whether the no-mul shortcuts for t=2/t=3 combined with Barrett for t>=0 improve on Exp03 GPU numbers — especially for a*b*c where Exp03 GPU was already good (1.136 ms) and Exp04 GPU was slightly better (1.107 ms).

Output: experiment_gpu.md with a new Experiment 05 section: T4 vars20 results, delta vs Exp03 GPU, CPU vs GPU delta using Exp05 CPU numbers from Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/oud/Projects/ECE-9413/assignment2/experiment_gpu.md
@/Users/oud/Projects/ECE-9413/assignment2/CLAUDE.md

GPU reference numbers for delta calculation (Exp03 GPU vars20 median):
- a: 0.465 ms
- a*b: 0.684 ms
- a*b + c: 1.187 ms
- a*b*c: 1.136 ms

Exp04 GPU numbers (teammate barrett_v9, for context):
- a: 0.450 ms (−3.2% vs Exp03 GPU)
- a*b: 0.729 ms (+6.6% vs Exp03 GPU — regression)
- a*b + c: 1.335 ms (+12.5% vs Exp03 GPU — regression)
- a*b*c: 1.107 ms (−2.6% vs Exp03 GPU)

Exp05 GPU uses _sumcheck_32_barrett_exp05 which has:
- Barrett ops instead of % q throughout
- No-mul shortcuts for t=2/t=3 (inline uint64, avoids element-wise % q in the per-element shortcuts — these are kept as XLA % since they are a single expression per element)
- Running-add for t>=4 (mod_add_branchless replacing mle_update_barrett for high-degree t points)
- Deferred scalar reduction (Barrett reduce at end of each t-point)

Expected GPU behavior: a*b+c regression from Exp04 (term-by-term dual-reduction) may not apply in Exp05 because Exp05 uses single deferred reduction per t-point (one _barrett_reduce_u64_to_u32 call per t, not one per term). This may recover the a*b+c regression seen in Exp04.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push exp05 branch and run Modal GPU benchmark</name>
  <files>experiment_gpu.md</files>
  <action>
Ensure the exp05 branch and current student.py state are accessible from Modal (or the local repo is already mounted). Check the modal_run.py setup:

```bash
cd /Users/oud/Projects/ECE-9413/assignment2
git branch --show-current   # must be exp05
git log --oneline -1         # record hash
cat modal_run.py | head -40  # verify app name and how code is mounted
```

Run the GPU benchmark on T4. modal_run.py delegates to tests.benchmark via subprocess and runs all three num-vars tiers per invocation:

```bash
cd /Users/oud/Projects/ECE-9413/assignment2
modal run modal_run.py 2>&1 | tee /tmp/exp05_gpu.txt
```

If modal_run.py mounts the local directory (volume or local mount), the current student.py on exp05 branch will be used automatically.

If modal_run.py checks out a specific git hash and the exp05 branch is not pushed to remote, push first:
```bash
git push -u origin exp05
```

Then re-run modal_run.py with the exp05 branch hash.

After the Modal run completes, read /tmp/exp05_gpu.txt and extract for vars20:
- Compile (ms), Median (ms), p90 (ms), Mpts/s for each of: a, a*b, a*b + c, a*b*c
  </action>
  <verify>
    <automated>grep -i "median\|ms" /tmp/exp05_gpu.txt 2>&1 | head -10</automated>
  </verify>
  <done>/tmp/exp05_gpu.txt contains benchmark timing output (lines matching "Median" or "ms") for vars20 with numbers for all four expressions.</done>
</task>

<task type="auto">
  <name>Task 2: Write Experiment 05 GPU section to experiment_gpu.md</name>
  <files>experiment_gpu.md</files>
  <action>
Append a new "Experiment 05 GPU — no-mul shortcuts + Barrett + running-add t>=4" section to experiment_gpu.md following the format of prior GPU sections.

The section must include:

1. Git hash (from exp05 branch `git log --oneline -1`)
2. "What changes" — same synthesis description as experiment.md but highlighting the GPU-specific aspect: Barrett reduction is now applied everywhere in the GPU path; the no-mul shortcuts for t=2/t=3 are kept (their inline uint64 expressions are a single XLA op per element, not improved by Barrett, but kept for correctness and simplicity); running-add for t>=4 uses _mod_add_branchless_u32; the deferred scalar reduction per t-point uses _barrett_reduce_u64_to_u32 (single Barrett reduce at end instead of per-term).
3. Results — num-vars 20 table (from Modal T4 output). Only vars20 is required; include vars4 and vars16 if the Modal run covers them.
4. Delta vs GPU Experiment 03 (N=20 median) table:
   ```
   | Expression | Exp 03 GPU (ms) | Exp 05 GPU (ms) | Delta |
   ```
5. CPU vs GPU delta table using Exp05 CPU numbers from experiment.md Plan 02:
   ```
   | Expression | CPU Exp05 (ms) | GPU Exp05 (ms) | Delta |
   ```
6. Analysis — explain GPU-specific behavior: whether the no-mul shortcuts for t=2/t=3 help on GPU (they reduce element-wise ops but % q is an IDIV on GPU, so the inline formulas may be neutral or slightly better than Barrett mul if q is constant), whether deferred reduction with single Barrett reduce per t-point avoids the dual-reduction regression seen in Exp04 for a*b+c.

After writing the section, commit:
```bash
cd /Users/oud/Projects/ECE-9413/assignment2
git add experiment_gpu.md
git commit -m "docs(phase-7): record Exp05 GPU benchmark results in experiment_gpu.md"
```
  </action>
  <verify>
    <automated>cd /Users/oud/Projects/ECE-9413/assignment2 && grep -c "Experiment 05" experiment_gpu.md</automated>
  </verify>
  <done>experiment_gpu.md contains "Experiment 05" section with GPU T4 results for vars20, delta vs Exp03 GPU, and CPU vs GPU delta. Numbers are real benchmark output. Committed on exp05 branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modal remote execution | Code runs on Modal T4 GPU; output piped back locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | GPU benchmark output | accept | Numbers sourced from modal run stdout captured to /tmp/exp05_gpu.txt; cross-check by re-reading the file before writing to experiment_gpu.md |
| T-07-03-02 | Denial of Service | Modal GPU availability | accept | If Modal T4 unavailable, note in experiment_gpu.md that GPU run was blocked and proceed to Plan 04 |
</threat_model>

<verification>
- `grep "Experiment 05" experiment_gpu.md` matches at least once
- experiment_gpu.md contains a table row for `a*b*c` under "Experiment 05"
- experiment_gpu.md contains "CPU vs GPU Delta" under "Experiment 05"
- `git log --oneline -3` shows the docs commit on exp05 branch
</verification>

<success_criteria>
- experiment_gpu.md has complete Experiment 05 GPU section: git hash, what-changes, vars20 T4 result table, delta-vs-Exp03-GPU table, CPU-vs-GPU table, analysis
- All numbers are real Modal T4 output (not placeholder or fabricated)
- Committed on exp05 branch
</success_criteria>

<output>
After completion, create `.planning/phases/07-exp05-incorporate-teammate-v9-optimizations-learn-from-exp01/07-03-SUMMARY.md`
</output>
