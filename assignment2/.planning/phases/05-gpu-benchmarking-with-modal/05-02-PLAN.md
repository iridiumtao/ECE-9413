---
phase: "05-gpu-benchmarking-with-modal"
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - experiment_gpu.md
autonomous: false
requirements:
  - GPU-02
  - PERF-02

must_haves:
  truths:
    - "experiment_gpu.md exists at the project root"
    - "experiment_gpu.md has three sections matching experiment.md: Baseline, Experiment 01, Experiment 02"
    - "Each section records the same git hash as experiment.md"
    - "Each section has three result tables for num-vars 4, 16, 20"
    - "Each table has columns: Expression | Compile (ms) | Median (ms) | p90 (ms) | Mpts/s"
    - "File header says Device: GPU (Modal T4)"
    - "Each section ends with a CPU vs GPU delta table comparing N=20 median vs the CPU values from experiment.md"
    - "student.py is restored to HEAD after each hash checkout"
  artifacts:
    - path: "experiment_gpu.md"
      provides: "GPU benchmark results for all three experiments"
      contains: "GPU (Modal T4)"
    - path: "experiment_gpu.md"
      provides: "Baseline section with git hash"
      contains: "632526c70eb2cdf398d75ba30afdfd40ff49933d"
    - path: "experiment_gpu.md"
      provides: "Experiment 01 section with git hash"
      contains: "86e0b5f94092bb3e7a8344ee7de04d0057f0d336"
    - path: "experiment_gpu.md"
      provides: "Experiment 02 section with git hash"
      contains: "2c31b3ed2afefa129b5ce9d6bb5ad50d1aa786e2"
  key_links:
    - from: "experiment_gpu.md delta tables"
      to: "experiment.md CPU values"
      via: "manual copy of CPU medians for N=20"
      pattern: "CPU.*ms.*GPU.*ms.*Delta|Baseline.*ms.*GPU.*ms"
---

<objective>
Run all three experiments (Baseline, Experiment 01, Experiment 02) on Modal T4 GPU by
checking out each tagged git hash into `student.py`, invoking `modal run modal_run.py`,
recording the output, and writing `experiment_gpu.md` at the project root.

Purpose: Provide GPU benchmark data to match the CPU data in `experiment.md`, enabling
a like-for-like CPU vs GPU comparison for the course report.

Output: `experiment_gpu.md` at project root, mirroring `experiment.md` structure with
GPU (Modal T4) results and CPU vs GPU delta tables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-gpu-benchmarking-with-modal/05-CONTEXT.md
@.planning/phases/05-gpu-benchmarking-with-modal/05-01-SUMMARY.md

<interfaces>
<!-- From experiment.md — CPU values used for delta computation -->

Baseline N=20 CPU medians (from experiment.md):
  a         → 2.098 ms
  a*b       → 6.571 ms
  a*b + c   → 15.693 ms
  a*b*c     → 9.968 ms

Experiment 01 N=20 CPU medians (from experiment.md):
  a         → 1.145 ms
  a*b       → 3.864 ms
  a*b + c   → 5.241 ms
  a*b*c     → 6.508 ms

Experiment 02 N=20 CPU medians (from experiment.md):
  a         → 1.091 ms
  a*b       → 3.800 ms
  a*b + c   → 5.538 ms
  a*b*c     → 6.667 ms

Git hashes (from CONTEXT.md D-11):
  Baseline:      632526c70eb2cdf398d75ba30afdfd40ff49933d
  Experiment 01: 86e0b5f94092bb3e7a8344ee7de04d0057f0d336
  Experiment 02: 2c31b3ed2afefa129b5ce9d6bb5ad50d1aa786e2

Per-hash workflow (D-10):
  git checkout <hash> -- student.py
  modal run modal_run.py
  (record stdout into the appropriate section)
  git checkout HEAD -- student.py

experiment_gpu.md format (D-12, D-13, D-14):
  - Header: "Device: GPU (Modal T4)" instead of "Device: CPU (Apple Silicon, no GPU)"
  - Same section headings and table columns as experiment.md
  - Delta table at end of each section: CPU median vs GPU median for N=20 only
  - Delta = GPU_median - CPU_median; format as "+X.XXX ms (+Y.Y%)" or "−X.XXX ms (−Y.Y%)"
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run all three experiments on Modal T4 and record raw output</name>

  <read_first>
    - /Users/oud/Projects/ECE-9413/assignment2/experiment.md — source of CPU values and section structure to mirror
    - /Users/oud/Projects/ECE-9413/assignment2/.planning/phases/05-gpu-benchmarking-with-modal/05-CONTEXT.md — D-10, D-11: per-hash checkout workflow and the three exact hashes
    - /Users/oud/Projects/ECE-9413/assignment2/modal_run.py — confirm it exists and review the benchmark command it will run
  </read_first>

  <what-built>
    `modal_run.py` is ready (Wave 1). The executor has prepared the following commands —
    the human must run them in the terminal (requires an active `modal` CLI session and GPU credits).
  </what-built>

  <how-to-verify>
    Run the following three sequences in order. After each sequence, copy the full terminal
    output (the Rich tables) and paste it into a scratch document — it will be needed in Task 2.

    **Sequence 1 — Baseline**
    ```bash
    cd /Users/oud/Projects/ECE-9413/assignment2
    git checkout 632526c70eb2cdf398d75ba30afdfd40ff49933d -- student.py
    modal run modal_run.py
    git checkout HEAD -- student.py
    ```
    Expected: Three Rich tables (N=4, N=16, N=20) printed to stdout.

    **Sequence 2 — Experiment 01**
    ```bash
    cd /Users/oud/Projects/ECE-9413/assignment2
    git checkout 86e0b5f94092bb3e7a8344ee7de04d0057f0d336 -- student.py
    modal run modal_run.py
    git checkout HEAD -- student.py
    ```
    Expected: Three Rich tables (N=4, N=16, N=20).

    **Sequence 3 — Experiment 02**
    ```bash
    cd /Users/oud/Projects/ECE-9413/assignment2
    git checkout 2c31b3ed2afefa129b5ce9d6bb5ad50d1aa786e2 -- student.py
    modal run modal_run.py
    git checkout HEAD -- student.py
    ```
    Expected: Three Rich tables (N=4, N=16, N=20).

    After running all three sequences:
    - Confirm `git status` shows no modified files (student.py is at HEAD)
    - Keep all three outputs available for Task 2

    IMPORTANT: If `modal run modal_run.py` fails with a JAX import error, try:
    ```bash
    modal run modal_run.py --bench-only
    ```
    (skips pytest; runs benchmarks directly — tests can be run separately if needed)
  </how-to-verify>

  <acceptance_criteria>
    - All three sequences completed without error
    - `git status` shows `student.py` is clean (not modified)
    - Three sets of benchmark tables recorded (9 tables total: 3 experiments × 3 num-vars tiers)
    - Each table has 4 rows (expressions: a, a*b, a*b+c, a*b*c) and columns: Expression, Compile (ms), Median (ms), p90 (ms), Mpts/s
  </acceptance_criteria>

  <resume-signal>
    Paste the raw terminal output from all three `modal run` invocations here, then type "done".
    Example format:

    --- BASELINE OUTPUT ---
    [paste full terminal output for N=4, N=16, N=20]

    --- EXPERIMENT 01 OUTPUT ---
    [paste full terminal output]

    --- EXPERIMENT 02 OUTPUT ---
    [paste full terminal output]
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write experiment_gpu.md from recorded GPU output</name>
  <files>experiment_gpu.md</files>

  <read_first>
    - /Users/oud/Projects/ECE-9413/assignment2/experiment.md — exact format to mirror; read the full file to extract CPU N=20 median values for each experiment's delta table
    - /Users/oud/Projects/ECE-9413/assignment2/.planning/phases/05-gpu-benchmarking-with-modal/05-CONTEXT.md — D-12 (mirror format), D-13 (header), D-14 (delta table)
    - The raw GPU output pasted by the human in Task 1's resume-signal — use these exact numbers
  </read_first>

  <action>
Create `/Users/oud/Projects/ECE-9413/assignment2/experiment_gpu.md` with the following structure.
Use the exact GPU numbers from the Task 1 output. Do NOT invent or approximate numbers —
copy them exactly from the terminal output.

**File structure:**

```markdown
# Experiment Log — SumCheck Prover Optimization (GPU)

Each experiment records what changed, the git hash at the time of measurement, and a result summary table.
All benchmarks run with: `modal run modal_run.py` (invokes `python -m tests.benchmark --bench --bits 32 --num-vars {N} --runs 8 --warmup 3` on Modal)
Device: GPU (Modal T4)

---

## Baseline — Current implementation (pre-optimization)

**Git hash:** `632526c70eb2cdf398d75ba30afdfd40ff49933d`

**What this is:**
- `@jax.jit` on `sumcheck_32`
- No `t=0`/`t=1` shortcut — calls `mle_update_32` for every evaluation point including `v=0` and `v=1`
- Recomputes `tbl[::2]` / `tbl[1::2]` separately in fold step (no evens/odds reuse)
- Always naive `% q` reduction
- `claim0` derived from `round_evals[0][0] + round_evals[0][1]`

### Results — num-vars 4 (N=16)

| Expression | Compile (ms) | Median (ms) | p90 (ms) | Mpts/s |
|---|---|---|---|---|
| `a` | [from GPU output] | [from GPU output] | [from GPU output] | [from GPU output] |
...

### Results — num-vars 16 (N=65,536)
[same table]

### Results — num-vars 20 (N=1,048,576)
[same table]

### CPU vs GPU — N=20 median

| Expression | CPU (ms) | GPU T4 (ms) | Delta |
|---|---|---|---|
| `a` | 2.098 | [GPU value] | [±X.XXX ms (±Y.Y%)] |
| `a*b` | 6.571 | [GPU value] | [±X.XXX ms (±Y.Y%)] |
| `a*b + c` | 15.693 | [GPU value] | [±X.XXX ms (±Y.Y%)] |
| `a*b*c` | 9.968 | [GPU value] | [±X.XXX ms (±Y.Y%)] |

---

## Experiment 01 — t=0/t=1 shortcut

**Git hash:** `86e0b5f94092bb3e7a8344ee7de04d0057f0d336`

[same What changes bullet list as experiment.md — copy verbatim]

### Results — num-vars 4 / 16 / 20 [tables from GPU output]

### CPU vs GPU — N=20 median

| Expression | CPU (ms) | GPU T4 (ms) | Delta |
|---|---|---|---|
| `a` | 1.145 | [GPU value] | [±X.XXX ms (±Y.Y%)] |
| `a*b` | 3.864 | [GPU value] | [±X.XXX ms (±Y.Y%)] |
| `a*b + c` | 5.241 | [GPU value] | [±X.XXX ms (±Y.Y%)] |
| `a*b*c` | 6.508 | [GPU value] | [±X.XXX ms (±Y.Y%)] |

---

## Experiment 02 — evens/odds pre-computation reuse

**Git hash:** `2c31b3ed2afefa129b5ce9d6bb5ad50d1aa786e2`

[same What changes bullet list as experiment.md — copy verbatim]

### Results — num-vars 4 / 16 / 20 [tables from GPU output]

### CPU vs GPU — N=20 median

| Expression | CPU (ms) | GPU T4 (ms) | Delta |
|---|---|---|---|
| `a` | 1.091 | [GPU value] | [±X.XXX ms (±Y.Y%)] |
| `a*b` | 3.800 | [GPU value] | [±X.XXX ms (±Y.Y%)] |
| `a*b + c` | 5.538 | [GPU value] | [±X.XXX ms (±Y.Y%)] |
| `a*b*c` | 6.667 | [GPU value] | [±X.XXX ms (±Y.Y%)] |
```

**Delta computation rule:**
- Delta = GPU_median - CPU_median
- Positive (GPU slower): format as `+X.XXX ms (+Y.Y%)`
- Negative (GPU faster): format as `−X.XXX ms (−Y.Y%)`
- Percentage: `abs(delta / cpu_median) * 100` rounded to 1 decimal place

**Mandatory accuracy rule:** Every number in a result table MUST come from the Task 1 GPU
terminal output. Do NOT copy CPU numbers from experiment.md into the result tables. The
CPU column in the delta table at the bottom of each section is the ONLY place CPU values appear.
  </action>

  <verify>
    <automated>
      cd /Users/oud/Projects/ECE-9413/assignment2 &amp;&amp;
      test -f experiment_gpu.md &amp;&amp;
      grep -c 'GPU (Modal T4)' experiment_gpu.md &amp;&amp;
      grep -c '632526c70eb2cdf398d75ba30afdfd40ff49933d' experiment_gpu.md &amp;&amp;
      grep -c '86e0b5f94092bb3e7a8344ee7de04d0057f0d336' experiment_gpu.md &amp;&amp;
      grep -c '2c31b3ed2afefa129b5ce9d6bb5ad50d1aa786e2' experiment_gpu.md &amp;&amp;
      grep -c 'CPU vs GPU' experiment_gpu.md
    </automated>
  </verify>

  <acceptance_criteria>
    - `experiment_gpu.md` exists at `/Users/oud/Projects/ECE-9413/assignment2/experiment_gpu.md`
    - `grep -c 'GPU (Modal T4)' experiment_gpu.md` returns >= 1
    - `grep '632526c70eb2cdf398d75ba30afdfd40ff49933d' experiment_gpu.md` matches (Baseline hash)
    - `grep '86e0b5f94092bb3e7a8344ee7de04d0057f0d336' experiment_gpu.md` matches (Experiment 01 hash)
    - `grep '2c31b3ed2afefa129b5ce9d6bb5ad50d1aa786e2' experiment_gpu.md` matches (Experiment 02 hash)
    - `grep -c '## Baseline\|## Experiment 01\|## Experiment 02' experiment_gpu.md` returns 3
    - `grep -c 'CPU vs GPU' experiment_gpu.md` returns 3 (one delta table per experiment section)
    - `grep -c 'num-vars 4\|num-vars 16\|num-vars 20' experiment_gpu.md` returns >= 9 (3 per section)
    - `grep -c '| \`a\`' experiment_gpu.md` returns >= 12 (4 expressions × 3 sections × at least in result tables)
    - No cell in the result tables contains a placeholder like "[from GPU output]" — all replaced with real numbers
  </acceptance_criteria>

  <done>
    `experiment_gpu.md` exists at the project root with:
    - Header "Device: GPU (Modal T4)"
    - Three experiment sections (Baseline, Exp 01, Exp 02) each with their correct git hash
    - Nine result tables (3 sections × 3 num-vars tiers) populated with real GPU numbers from Task 1
    - Three "CPU vs GPU — N=20 median" delta tables (one per section) with correct delta calculations
    - No placeholder text remaining
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → Modal cloud | `add_local_dir(".")` uploads the working tree including the temporarily checked-out `student.py` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | `git checkout <hash> -- student.py` during experiment | mitigate | Workflow explicitly runs `git checkout HEAD -- student.py` after each experiment run; executor MUST verify `git status` is clean before proceeding to the next hash |
| T-05-06 | Information Disclosure | experiment_gpu.md numbers | accept | File contains only performance timing data; no secrets, no user data |
| T-05-07 | Repudiation | GPU output not saved before writing experiment_gpu.md | accept | Human pastes full output in Task 1 resume-signal; it is preserved in conversation history |
</threat_model>

<verification>
After completing both tasks, verify:

```bash
cd /Users/oud/Projects/ECE-9413/assignment2

# experiment_gpu.md exists and has required content
test -f experiment_gpu.md && echo "exists"
grep 'GPU (Modal T4)' experiment_gpu.md
grep '632526c70eb2cdf398d75ba30afdfd40ff49933d' experiment_gpu.md
grep '86e0b5f94092bb3e7a8344ee7de04d0057f0d336' experiment_gpu.md
grep '2c31b3ed2afefa129b5ce9d6bb5ad50d1aa786e2' experiment_gpu.md
grep -c 'CPU vs GPU' experiment_gpu.md   # must return 3

# student.py is at HEAD (not a checked-out historical version)
git status student.py   # must show: nothing to commit / not modified

# experiment.md is unchanged
git status experiment.md   # must show: nothing to commit / not modified
```
</verification>

<success_criteria>
- `experiment_gpu.md` exists at project root
- All three experiment git hashes appear in `experiment_gpu.md`
- File header contains "GPU (Modal T4)"
- Nine result tables are populated with real GPU timing numbers
- Three CPU vs GPU delta tables present, one per experiment section
- `student.py` is at HEAD after the plan completes
- `experiment.md` is unmodified
</success_criteria>

<output>
After completion, create `.planning/phases/05-gpu-benchmarking-with-modal/05-02-SUMMARY.md`
following the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
